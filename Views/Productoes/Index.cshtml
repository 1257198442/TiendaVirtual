@model IEnumerable<TiendaVirtual.Producto>

@{
    ViewBag.Title = "Index";
}

<h2>Producto</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    if (item.Stock.stockQuantity == 0)
    {
    }
    else { 
    <tr>
        <td>
            <img src="~/image/@Html.DisplayFor(modelItem => item.image)"height="100"width="100">
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price) €
        </td>
        <td>
            <div style="width:400px;word-wrap:break-word">
            @item.description
                </div>
        </td>
        
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id }) |
            @Html.ActionLink("Add to Cart", "AddProducto", "Compras", new { productoId=item.Id, compradoQuantity=1 }, null)
        </td>
    </tr>
        }
    }

</table>
